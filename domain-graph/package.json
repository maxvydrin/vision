{
  "name": "studio-domain-graph",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive graph of the Software Organization Domain Model (domains → blocks → terms, Studio coverage tiers, typed relationships with cardinality).",
  "scripts": {
    "parse": "node scripts/parse-model.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "cytoscape": "^3.30.2",
    "cytoscape-expand-collapse": "^4.1.1",
    "cytoscape-fcose": "^2.2.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
